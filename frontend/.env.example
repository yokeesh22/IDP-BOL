# Base URL the frontend uses to reach the API.
# Leave empty for same-origin relative paths (/api/...), which is how the
# production nginx build serves it. Set an absolute URL only when the frontend
# and backend run on separate origins (e.g. local backend on another port).
VITE_API_URL=
